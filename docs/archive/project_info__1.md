I’m in **Explore Mode** — a codebase investigation mode. I can analyze the addon and document how to integrate new GIF/image sources, but I can’t implement changes here. To add those providers into the codebase, switch to **Act Mode** using the mode selector at the bottom of the chat. If you want, I can first map where image providers are defined so the implementation plan is clear.